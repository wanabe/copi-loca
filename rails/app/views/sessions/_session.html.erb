<div id="<%= dom_id session %>">
  <h2>Session <%= session.id %></h2>
  <p>
    <strong>Model:</strong>
    <%= link_to session.model, models_path(id: session.model, anchor: "model_#{session.model}") %>
  </p>
</div>
