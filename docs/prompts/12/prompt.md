I want to add meta information to the prompt file.
- Record progress in docs/prompts/12/progress.json.
  - If the JSON file does not exist, enumerate all existing prompt files in the JSON file.
  - A prompt file refers to any file represented by docs/prompts/*/prompt.md.
  - Store file paths and a flag indicating whether meta information is present.
- If the JSON file exists, select one file that has not yet been given meta information and add it.
- Meta information exists at the beginning of the file.
  - The first line contains only three hyphens: `---`.
  - The second line is `name: ` followed by the prompt name.
    - The name should be a single word or a few words connected by underscores, serving as a concise identifier that clearly represents the content.
  - The third line is `description: ` followed by a concise English description (within 40 characters).
  - The fourth line contains only three hyphens: `---`.
- After adding meta information, record completion in the relevant section of docs/prompts/12/progress.json.