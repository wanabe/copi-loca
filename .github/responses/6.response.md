I'll proceed as follows:
1. Identify the highest-priority file from docs/missing_specs_checklist.json that lacks a spec.
2. Determine the correct spec file path and type (request spec for controllers, standard spec otherwise).
3. Write a spec for the target file, following the project conventions.
4. Update docs/missing_specs_checklist.json to mark the spec as added.

First, I'll review the checklist and an example spec to ensure proper format.

The highest-priority file without a spec is app/controllers/prompts_controller.rb#update. For controllers, request specs are required, one per action.

Next steps:
- Write a request spec for the update action: spec/requests/prompts/update_spec.rb.
- Mark the "update" action as spec_added: true in docs/missing_specs_checklist.json.

I'll now draft the spec and update the checklist.

Request spec for app/controllers/prompts_controller.rb#update has been added at spec/requests/prompts/update_spec.rb. The checklist is updated to mark the spec as added. Task complete.

